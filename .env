# 🌐 FRONTEND ENVIRONMENT VARIABLES

# Your backend base API URL
VITE_API_URL=https://socialearnbackend.onrender.com/api

# Optional: local fallback during development
# Uncomment this line when running locally
# VITE_API_URL=http://localhost:5000/api

# App name (optional, for branding in frontend)
VITE_APP_NAME=SocialEarn

# Default watch duration (in seconds) if not provided by backend
VITE_DEFAULT_WATCH_DURATION=30

# Socket connection URL (automatically uses base URL fallback)
VITE_SOCKET_URL=https://socialearnbackend.onrender.com

# Debug flag (for logging extra info in console)
VITE_DEBUG_MODE=true